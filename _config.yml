defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
sass:
  sass_dir: _sass/--foundation
exclude:
  - bin
  - CNAME
  - Gemfile
  - Gemfile.lock
  - node_modules
  - package.json
  - README.md
  - vendor
  - yarn.lock
